using UnityEngine;

public class PickupObject : MonoBehaviour
{
    /** TODO: Player interaction logic
     * If Player touches object, destroy or disable 
     * Play sound
     * Find manager of current level and increment "objects picked up" count ? **/

    void Start()
    {
        
    }
    void Update()
    {
        
    }
}
